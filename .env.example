# Layers Environment Configuration

# Supabase Configuration (required)
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key

# RevenueCat Configuration (required for monetization)
REVENUECAT_IOS_KEY=your_revenuecat_ios_key
REVENUECAT_ANDROID_KEY=your_revenuecat_android_key

# RevenueCat Export Credit Product IDs (optional - for per-export pricing)
# These are only needed if you want to customize the product IDs used for consumable purchases
# REVENUECAT_EXPORT_PRODUCT_ID_IOS=com.connectio.layers.export_credit
# REVENUECAT_EXPORT_PRODUCT_ID_ANDROID=layers_export_credit

# Note: AI processing (fal.ai BiRefNet) is handled server-side via BuildShip workflows.
# The FAL_API_KEY is configured in the BuildShip dashboard, not in this app.
