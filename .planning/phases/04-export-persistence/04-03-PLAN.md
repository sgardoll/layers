---
phase: 04-export-persistence
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [lib/services/layer_service.dart, lib/services/export_service.dart, lib/services/project_repository.dart]
autonomous: true
---

<objective>
Clean up deprecated services and finalize Supabase integration.

Purpose: Remove old Dio-based services that were replaced by Supabase equivalents.
Output: Clean codebase with only Supabase services, no dead code.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove deprecated services</name>
  <files>lib/services/layer_service.dart, lib/services/export_service.dart, lib/services/project_repository.dart</files>
  <action>Delete these files - they were replaced by supabase_project_service.dart and supabase_export_service.dart. Search for any remaining imports and update them to use Supabase equivalents. Check job_provider.dart - if it references old LayerService, update to use SupabaseProjectService.</action>
  <verify>flutter analyze lib/ shows no errors about missing imports</verify>
  <done>No deprecated service files remain, all imports updated</done>
</task>

<task type="auto">
  <name>Task 2: Update providers to use Supabase services</name>
  <files>lib/providers/job_provider.dart</files>
  <action>Check if job_provider.dart still references old LayerService. If so, update to use SupabaseProjectService. The job polling pattern changes: instead of polling REST API, subscribe to realtime updates on projects table. Update JobNotifier to use realtime subscription for status changes instead of Timer-based polling.</action>
  <verify>flutter analyze lib/providers/job_provider.dart shows no errors</verify>
  <done>JobNotifier uses Supabase realtime instead of polling, no references to old services</done>
</task>

</tasks>

<verification>
- [ ] flutter analyze lib/ - no errors
- [ ] No files reference layer_service.dart, export_service.dart, or project_repository.dart
- [ ] JobNotifier uses Supabase realtime subscriptions
</verification>

<success_criteria>
- Deprecated service files deleted
- All imports updated to Supabase equivalents
- JobNotifier uses realtime instead of polling
- flutter analyze passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-export-persistence/04-03-SUMMARY.md`
</output>
